Following steps needed to run scripts:

1.) Set CLASSPATH

#for f in `find ./pcells -type f -name '*.jar' `; do CLASSPATH=$CLASSPATH:$f; done

#### To include logback.xml
for f in `find ./pcells -type f -name '*.*' `; do echo "$f";CLASSPATH=$CLASSPATH:$f; done

2.) Execute scripts:

./wrapper.sh runConnection.jy

